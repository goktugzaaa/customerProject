/**
 * Telefon ÇEVRESİNDEKİ sayfa arka planı (lacivert/siyah).
 * Elle değiştireceksen: burayı + `app/globals.css` içindeki `:root { --viewport: ... }` aynı yap.
 */
export const VIEWPORT_OUTER_HEX = "#07070f";
