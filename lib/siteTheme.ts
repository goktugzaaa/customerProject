/**
 * Dış sayfa (telefon çevresi) arka planı.
 * Production’da ilk boyamada CSS değişkeni gecikirse beyaz flaş olmasın diye
 * layout / AppShell’de düz hex kullanılıyor; globals.css --viewport ile aynı tut.
 */
export const VIEWPORT_BACKGROUND = "#e8edf5";
